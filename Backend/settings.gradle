pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // Upgrading to 1.0.0 bypasses the broken IBM_SEMERU references completely
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'Pirates-Sea-Rail'